version: '3'

services:
  nginx-proxy:
    image: jwilder/nginx-proxy:latest
    ports:
      # web
      - 80:80
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro

networks:
  default:
    name: rori-stack